#include <iostream>
using std::cin;
using std::cout;
using std::endl;

#include <vector>
using std::vector;

int problem314() {
    vector<int> ints;
    int input;

    while(cin >> input)
        ints.push_back(input);

    return 0;
}